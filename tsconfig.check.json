// 类型检查用（不产出文件）：src + examples + utils 一起查。
// 构建用的 tsconfig.json 只管 src（rootDir 是 src，产出的 lib/ 才能直接当包用）。
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "."
  },
  "include": ["src", "examples", "utils"]
}
